interface Matrix {
    matrix: string[][];
    min: number;
    max: number
}

interface Environment {
    value: string[]
}

interface EnvironmentField {
    value: '#';
    x: number;
    y: number
}
